LoverOfTheLight-Stage
=====================

We have modified model.cc and Stage.hh in libstage folder in order to easily find the robot pointer.
Added void* myRobot;
Added getUserdata();
Added setUserData(void* robot);
